type: u32
help: send an options ping every x seconds (recomendent 50), failure will unregister and/or mark it down
syntax:expression: $VAR(@) > 24 && $VAR(@) < 601;
                   "Port must be between 25 and 600"
